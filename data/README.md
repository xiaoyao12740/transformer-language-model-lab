# Data

`tiny_corpus.txt` is an original miniature corpus used only for tests and CI. Formal training uses a normalized excerpt of Shakespeare's public-domain plays downloaded by `scripts/prepare_data.py`. The downloaded corpus is intentionally git-ignored; `data/provenance.json` records its URL, SHA-256, character count and vocabulary size.

